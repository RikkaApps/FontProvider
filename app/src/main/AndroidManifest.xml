<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="moe.shizuku.notocjk.provider">

    <permission android:name="moe.shizuku.notocjk.provider.BIND" android:protectionLevel="normal"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PreviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service
            android:name=".FontProviderService"
            android:enabled="true"
            android:exported="true"
            android:permission="moe.shizuku.notocjk.provider.BIND">

            <intent-filter>
                <action android:name="moe.shizuku.notocjk.provider.FontProviderService.BIND" />
            </intent-filter>

        </service>
    </application>

</manifest>